import { Prisma } from '@prisma/client';

const createBout = async (payload: any) => {
  return null;
};

const getAllBout = async () => {
  return null;
};

const getBoutById = async (id: string) => {
  return null;
};

const updateBout = async (id: string, payload: any) => {
  return null;
};

const deleteBout = async (id: string) => {
  return null;
};

export const BoutService = {
  createBout,
  getAllBout,
  getBoutById,
  updateBout,
  deleteBout,
};
