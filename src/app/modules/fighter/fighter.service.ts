import { Prisma } from '@prisma/client';

const createFighter = async (payload: any) => {
  return null;
};

const getAllFighter = async () => {
  return null;
};

const getFighterById = async (id: string) => {
  return null;
};

const updateFighter = async (id: string, payload: any) => {
  return null;
};

const deleteFighter = async (id: string) => {
  return null;
};

export const FighterService = {
  createFighter,
  getAllFighter,
  getFighterById,
  updateFighter,
  deleteFighter,
};
